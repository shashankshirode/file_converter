class ListFile {
  String? fileName;
  String? filePath;
  double? fileSize;

  ListFile(String? name, String? path, double? size) {
    fileName = name;
    filePath = path;
    fileSize = size;
  }
}
